services:
    prod:
        build:
            context: .
            target: production
            args:
                # Pass through a build-time variable; set in your shell or .env before building
        ports:
            - '3000:80'
        environment:
            - NODE_ENV=production
